# 🚀 Альтернатива: Развёртывание на Render.com

## Если Railway не работает - используйте Render.com

Render.com более стабилен и проще в настройке для Python проектов.

---

## Шаги развёртывания бота на Render.com (10 минут)

### 1. Регистрация

1. Откройте https://render.com
2. Нажмите **"Get Started for Free"**
3. Выберите **"Continue with GitHub"**
4. Авторизуйтесь через GitHub

### 2. Создание Web Service

⚠️ **Важно:** Используйте **Web Service** (не Background Worker - он платный!)

1. На главной странице Render нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите ваш GitHub репозиторий:
   - Если его нет в списке, нажмите **"Configure account"**
   - Дайте доступ к репозиторию `valorant-team-finder`
4. Нажмите **"Connect"** напротив вашего репозитория

### 3. Настройка сервиса

Заполните форму:

**Basic Settings:**
- **Name**: `valorant-bot` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)` (ближе к России)
- **Branch**: `main` (или `master`)
- **Root Directory**: `bot` ⚠️ **ВАЖНО!**
- **Runtime**: `Python 3`

**Build & Deploy:**
- **Build Command**: 
  ```
  pip install -r requirements.txt
  ```
- **Start Command**:
  ```
  python main.py
  ```

**Instance Type:**
- Выберите **"Free"** (0$/месяц)

### 4. Добавление переменных окружения

Прокрутите вниз до секции **"Environment Variables"**

Нажмите **"Add Environment Variable"** и добавьте три переменные:

**Переменная 1:**
```
Key: BOT_TOKEN
Value: ваш токен от @BotFather
```

**Переменная 2:**
```
Key: SUPABASE_URL
Value: https://ваш-проект.supabase.co
```

**Переменная 3:**
```
Key: SUPABASE_KEY
Value: eyJ... (ваш длинный ключ)
```

### 5. Создание сервиса

1. Нажмите **"Create Web Service"** внизу страницы
2. Render начнёт деплой (займёт 2-3 минуты)
3. Дождитесь статуса **"Live"** с зелёной точкой

### 6. Проверка логов

1. На странице вашего сервиса выберите вкладку **"Logs"**
2. Должно появиться:
   ```
   HTTP server started on port 10000
   Бот запущен!
   Application started
   ```
3. Если есть ошибки - смотрите их в логах

💡 **Примечание:** Бот открывает HTTP порт для Render health checks, но это только техническая необходимость. Сам бот работает через Telegram API.

⚠️ **Если видите ошибку "ResolutionImpossible" или конфликт зависимостей:**

Это означает конфликт версий библиотек. Решение:

1. В GitHub откройте `bot/requirements.txt`
2. Нажмите **Edit** (карандаш)
3. Замените на:
   ```
   python-telegram-bot>=20.0
   supabase>=2.0
   httpx>=0.24.0
   ```
4. Commit → Render автоматически перезапустит деплой
5. В логах должно появиться: `Бот запущен!`

---

## ⚠️ Важная особенность Render Free Tier

**Бот "засыпает" после 15 минут неактивности!**

Это означает:
- ✅ Когда кто-то пишет боту - он "просыпается" (занимает 30-60 секунд)
- ❌ Ежедневные уведомления НЕ будут работать, пока бот спит
- ✅ Все данные сохраняются

### Решение 1: Использовать Cron-job (бесплатно)

Чтобы бот не засыпал, можно "пинговать" его каждые 10 минут:

1. Откройте https://cron-job.org/en/
2. Зарегистрируйтесь (бесплатно)
3. Создайте новую задачу:
   - **Title**: Keep Valorant Bot Alive
   - **URL**: URL вашего сервиса в Render (например: `https://valorant-bot.onrender.com`)
   - **Schedule**: Every 10 minutes
   - **Enable**: ✅
4. Сохраните

Теперь бот будет всегда активен!

### Решение 2: Upgrade до Paid плана

- $7/месяц за постоянно работающий сервис
- Бот никогда не засыпает
- Ежедневные уведомления работают отлично

---

## Сравнение: Railway vs Render

| Характеристика | Railway | Render |
|---------------|---------|---------|
| Бесплатный план | $5 кредитов/месяц | Безлимитно |
| Время работы | 24/7 (пока есть кредиты) | Засыпает через 15 мин |
| Простота настройки | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Поддержка Python | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Логи | Удобные | Очень удобные |

**Рекомендация:**
- Если хотите полностью бесплатное решение БЕЗ ограничений → используйте Render + Cron-job
- Если готовы следить за кредитами Railway → используйте Railway
- Если можете заплатить $7/месяц → Render Paid или Railway Paid

---

## Миграция с Railway на Render

Если вы уже настроили Railway, но хотите перейти на Render:

1. Ничего не удаляйте в Railway (пусть остаётся как backup)
2. Создайте новый Web Service в Render (инструкция выше)
3. Проверьте, что всё работает
4. Если всё ок - можете удалить проект в Railway

---

## Проверка работы

1. **Откройте Telegram**
2. **Напишите боту:** `/start`
3. **Первый запрос займёт 30-60 секунд** (бот просыпается)
4. **Потом всё работает быстро**

### Настройка Cron-job для постоянной работы

Если вы настроили Cron-job:
- Бот будет работать 24/7
- Ежедневные уведомления будут приходить вовремя
- Не нужно ждать пробуждения

---

## Полная архитектура с Render

```
┌─────────────────┐
│   GitHub        │
│   (код)         │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼────┐ ┌──▼───────┐
│ Vercel │ │  Render  │
│  (Web) │ │  (Bot)   │
└───┬────┘ └──┬───────┘
    │         │
    └────┬────┘
         │
    ┌────▼────┐
    │Supabase │
    │  (DB)   │
    └─────────┘
```

Всё бесплатно! 🎉

---

## Итоговый чеклист для Render

- [ ] Аккаунт создан на Render.com
- [ ] Web Service создан и привязан к GitHub
- [ ] Root Directory = `bot`
- [ ] Build Command = `pip install -r requirements.txt`
- [ ] Start Command = `python main.py`
- [ ] Три переменные окружения добавлены
- [ ] Статус сервиса: Live (зелёная точка)
- [ ] В логах написано: "Бот запущен!"
- [ ] Бот отвечает на /start в Telegram
- [ ] (Опционально) Настроен Cron-job для постоянной работы

**Если все галочки стоят - готово!** ✅
