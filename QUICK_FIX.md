# 🚨 Быстрые решения частых проблем

## ❌ Ошибка в Vercel: "Environment Variable references Secret"

### Что случилось?
Vercel не может найти переменные окружения SUPABASE_URL и SUPABASE_KEY.

### Быстрое решение (3 минуты):

1. **Перейдите в Settings проекта:**
   - Откройте ваш проект в Vercel
   - Нажмите вкладку **"Settings"** (вверху)
   
2. **Добавьте переменные:**
   - В левом меню выберите **"Environment Variables"**
   - Нажмите **"Add New"** или кнопку с плюсом
   
3. **Первая переменная:**
   ```
   Name: SUPABASE_URL
   Value: https://ваш-проект.supabase.co
   Environment: All (отметить все галочки)
   ```
   Нажмите **"Save"**
   
4. **Вторая переменная:**
   ```
   Name: SUPABASE_KEY
   Value: eyJ... (ваш длинный ключ из Supabase)
   Environment: All (отметить все галочки)
   ```
   Нажмите **"Save"**

5. **Redeploy:**
   - Перейдите на вкладку **"Deployments"**
   - Найдите последний деплой (самый верхний)
   - Нажмите **три точки "..."** справа
   - Выберите **"Redeploy"**
   - Подтвердите → **"Redeploy"**

6. **Дождитесь завершения** (1-2 минуты)
   - Статус должен стать ✅ **"Ready"**

### Где взять эти значения?

**SUPABASE_URL:**
1. Откройте https://supabase.com
2. Выберите ваш проект
3. Settings (⚙️) → API
4. Project URL (например: `https://abcdefgh.supabase.co`)

**SUPABASE_KEY:**
1. Там же в Settings → API
2. anon public key
3. Скопируйте весь ключ (начинается с `eyJ...`)

---

## ❌ Vercel: Ошибка 500 при обращении к API

### Что случилось?
API не может подключиться к Supabase.

### Причины:
1. **Не добавлены переменные окружения** (самая частая)
2. Неправильная конфигурация `vercel.json`

### Решение 1: Проверьте переменные окружения

1. Откройте Vercel → ваш проект
2. **Settings** → **Environment Variables**
3. Должны быть две переменные:
   - `SUPABASE_URL` = `https://ваш-проект.supabase.co`
   - `SUPABASE_KEY` = `eyJ...` (длинный ключ)
4. Убедитесь, что для каждой выбрано **All** (Production + Preview + Development)
5. Если переменных нет или они неправильные - добавьте/исправьте
6. **Deployments** → **Redeploy**

### Решение 2: Обновите vercel.json

Если используете старый `vercel.json` с секцией `builds`, замените его на:

```json
{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
```

И переместите `public/index.html` в корень проекта (просто `index.html`).

### Проверка:

Откройте `https://ваш-сайт.vercel.app/api/health` в браузере.

Должно показать:
```json
{"success": true, "status": "healthy", ...}
```

Если видите это - API работает! ✅

---

## ⚠️ Vercel: Warning "Due to builds existing..."

### Что это значит?
Render видит, что бот работает, но не может найти открытый HTTP порт.

### Это проблема?
**НЕТ!** Если в логах есть:
```
Бот запущен!
Application started
```

То бот **работает нормально**! Telegram бот не нуждается в HTTP порте.

### Хотите убрать предупреждение?

Код уже обновлён - бот теперь открывает порт 10000 для Render.

После обновления кода на GitHub в логах появится:
```
HTTP server started on port 10000
Бот запущен!
```

И предупреждение исчезнет! ✅

### Проверка что бот работает:

Откройте Telegram и напишите боту `/start` - если он отвечает, всё отлично! 🎉

---

## ❌ Render: "AttributeError: 'NoneType' object has no attribute 'run_daily'"

### Что случилось?
Бот пытается использовать `JobQueue` (для ежедневных уведомлений), но он не установлен.

### Решение (2 минуты):

**В GitHub измените `bot/requirements.txt`:**

1. Откройте `bot/requirements.txt`
2. Нажмите **карандаш** (Edit)
3. Замените первую строку:

**Было:**
```
python-telegram-bot>=20.0
```

**Стало:**
```
python-telegram-bot[job-queue]>=20.0
```

4. **Commit changes**
5. Render автоматически перезапустит деплой
6. В логах должно появиться: `Бот запущен!` ✅

### Что делает `[job-queue]`?

Это устанавливает дополнительные зависимости для планирования задач:
- `APScheduler` - планировщик задач
- `pytz` - поддержка часовых поясов

Благодаря этому бот сможет отправлять ежедневные уведомления в 10:00 и 18:00.

---

## ❌ Render/Railway: "ResolutionImpossible" или ошибки зависимостей

### Что случилось?
Конфликт версий Python библиотек при установке зависимостей.

### Решение (2 минуты):

**Способ 1: Использовать упрощённый requirements.txt**

1. В GitHub откройте файл `bot/requirements.txt`
2. Нажмите иконку **карандаша** (Edit)
3. Замените содержимое на:
   ```
   python-telegram-bot>=20.0
   supabase>=2.0
   httpx>=0.24.0
   ```
4. **Commit changes**
5. Render/Railway автоматически перезапустит деплой

**Способ 2: Указать конкретные совместимые версии**

Замените содержимое `bot/requirements.txt` на:
```
python-telegram-bot==20.7
supabase==2.3.4
postgrest==0.16.0
httpx==0.25.2
```

**Способ 3: Установка без зависимостей (если всё ещё не работает)**

В настройках Render:
- **Build Command** измените на:
  ```
  pip install --no-deps python-telegram-bot supabase && pip install httpx
  ```

### Проверка:

После редеплоя в логах должно быть:
- ✅ `Successfully installed python-telegram-bot supabase ...`
- ✅ `Бот запущен!`

---

## ❌ Railway: "python: command not found"

### Что случилось?
Railway не может найти Python или использует неправильные настройки билда.

### Решение (5 минут):

**Способ 1: Удалить и пересоздать проект (проще)**

1. В Railway удалите текущий проект:
   - Откройте проект
   - Settings (внизу слева)
   - Прокрутите вниз → **"Delete Service"**
   - Подтвердите

2. Создайте новый проект:
   - **New Project** → **"Deploy from GitHub repo"**
   - Выберите `valorant-team-finder`
   - Railway автоматически определит настройки из `nixpacks.toml`

3. Добавьте переменные окружения:
   - Variables → Add Variable
   - `BOT_TOKEN`, `SUPABASE_URL`, `SUPABASE_KEY`

4. Дождитесь деплоя

**Способ 2: Исправить настройки текущего проекта**

1. Перейдите в **Settings** проекта
2. Найдите секцию **"Deploy"** или **"Build"**
3. Удалите все custom настройки:
   - **Root Directory**: оставьте ПУСТЫМ (не `bot`!)
   - **Build Command**: оставьте ПУСТЫМ
   - **Start Command**: оставьте ПУСТЫМ
4. Нажмите **"Save"**
5. Перейдите в **"Deployments"**
6. Нажмите **"Redeploy"** → **"Redeploy"**

**Способ 3: Явно указать команду**

Если способы 1 и 2 не помогли:

1. Settings → Deploy
2. **Start Command** измените на:
   ```
   python bot/main.py
   ```
3. **Install Command** (если есть):
   ```
   pip install -r bot/requirements.txt
   ```
4. Save → Redeploy

### Проверка:

После деплоя откройте логи:
- Должно быть: `Бот запущен!`
- НЕ должно быть: `python: command not found`

---

## ❌ Бот не отвечает в Telegram

### Быстрая проверка:

1. **Railway работает?**
   - Откройте https://railway.app
   - Выберите ваш проект
   - Перейдите в **"Deployments"**
   - Статус должен быть ✅ **SUCCESS**

2. **Посмотрите логи:**
   - Нажмите на последний деплой
   - Кнопка **"View Logs"**
   - Должно быть написано: `Бот запущен!`
   - Не должно быть красных ошибок

3. **Проверьте переменные:**
   - Перейдите во вкладку **"Variables"**
   - Должны быть 3 переменные:
     - `BOT_TOKEN` (токен от @BotFather)
     - `SUPABASE_URL` (URL Supabase)
     - `SUPABASE_KEY` (ключ Supabase)

### Если переменных нет - добавьте:

1. Во вкладке **"Variables"**
2. Нажмите **"New Variable"**
3. Добавьте по очереди все три переменные
4. Railway автоматически перезапустит бот

---

## ❌ Веб-приложение не показывает игроков

### Проверка в браузере:

1. Откройте веб-приложение
2. Нажмите **F12** (или ПКМ → "Inspect")
3. Перейдите на вкладку **"Console"**
4. Нажмите кнопку **"Обновить"** на сайте
5. Смотрите на ошибки

### Частые ошибки:

**Ошибка: "Failed to fetch" или "Network error"**
- Проверьте, что API работает: откройте `https://ваш-сайт.vercel.app/api/health`
- Должен быть JSON: `{"success": true, ...}`
- Если 404 или 500 - проверьте переменные окружения в Vercel

**Ошибка: "CORS"**
- Это нормально на localhost
- На Vercel должно работать автоматически

**Пустой список игроков:**
- Убедитесь, что хотя бы один игрок:
  1. Зарегистрировался в боте
  2. Ответил "Да, буду играть!" на вопрос бота
- Проверьте данные в Supabase:
  - Table Editor → `players` (должны быть записи)
  - Table Editor → `daily_status` (должны быть записи с is_playing = true)

---

## ❌ Railway: "Out of credits"

### Что случилось?
Закончились бесплатные $5 кредитов на месяц.

### Решение 1: Подождать
- Кредиты обновляются 1-го числа каждого месяца
- Данные сохранены, бот просто спит

### Решение 2: Добавить карту
- Railway → Settings → Usage
- Добавить карту (не спишут деньги, просто проверка)
- Получите дополнительные кредиты

### Решение 3: Перенести на Render.com (бесплатно)

1. Откройте https://render.com
2. Sign Up → Continue with GitHub
3. New → **"Web Service"**
4. Подключите репозиторий
5. Настройки:
   - **Name**: valorant-bot
   - **Root Directory**: `bot`
   - **Runtime**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
6. **Environment Variables** → Add from .env
   - Добавьте все три переменные
7. **Create Web Service**

⚠️ На Render бот будет "засыпать" через 15 минут неактивности, но проснётся при первом сообщении.

---

## ✅ Проверка что всё работает

### Чеклист за 2 минуты:

1. **Vercel:**
   - [ ] Проект открывается по ссылке
   - [ ] Нет ошибок на странице
   - [ ] Кнопка "Обновить" работает

2. **Railway/Render:**
   - [ ] Статус деплоя: SUCCESS
   - [ ] В логах написано: "Бот запущен!"
   - [ ] Нет красных ошибок

3. **Telegram бот:**
   - [ ] Бот отвечает на /start
   - [ ] Можно создать профиль
   - [ ] Можно отметиться "Да, буду играть"

4. **База данных:**
   - [ ] Supabase → Table Editor → players (есть записи)
   - [ ] Supabase → Table Editor → daily_status (есть записи)

5. **Веб-приложение:**
   - [ ] Открывается статистика (цифры не "-")
   - [ ] Показывается список игроков
   - [ ] Работают ссылки на Telegram профили

Если все галочки стоят - **СИСТЕМА РАБОТАЕТ!** 🎉

---

## 📞 Если ничего не помогло

1. Проверьте все переменные окружения (правильные ли значения)
2. Посмотрите логи в Railway и Vercel
3. Проверьте, что таблицы в Supabase созданы правильно:
   ```sql
   SELECT * FROM players;
   SELECT * FROM daily_status;
   ```
4. Убедитесь, что у бота правильный токен (проверьте в @BotFather)

---

## 🎯 Самая частая причина проблем

**90% проблем из-за неправильных переменных окружения!**

Проверьте внимательно:
- Нет лишних пробелов в начале/конце
- URL Supabase заканчивается на `.supabase.co`
- Ключ Supabase начинается с `eyJ`
- Токен бота содержит цифры и символ `:`

**Пример правильных значений:**
```
SUPABASE_URL=https://abcdefghijk.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```
