# 🚀 VALORANT Team Finder - Полная инструкция по облачному развёртыванию

## 🎯 Что мы будем использовать (всё БЕСПЛАТНО)

- **Supabase** - База данных PostgreSQL (бесплатно навсегда)
- **Vercel** - Хостинг веб-приложения и API (бесплатно навсегда)
- **Railway** - Хостинг Telegram бота (бесплатно $5 кредитов в месяц)
- **GitHub** - Хранение кода (бесплатно)

**Всё будет работать 24/7 без вашего участия!**

---

## 📅 Пошаговая инструкция

### ШАГ 1: Создание базы данных в Supabase (10 минут)

#### 1.1 Регистрация
1. Откройте https://supabase.com
2. Нажмите **"Start your project"**
3. Войдите через **GitHub** (или создайте аккаунт)

#### 1.2 Создание проекта
1. Нажмите **"New Project"**
2. Выберите организацию (или создайте новую)
3. Заполните:
   - **Name**: `valorant-team-finder`
   - **Database Password**: придумайте надёжный пароль (СОХРАНИТЕ ЕГО!)
   - **Region**: `Europe West` (ближайший к России)
   - **Pricing Plan**: `Free` (0$/месяц)
4. Нажмите **"Create new project"**
5. Подождите 2-3 минуты (база данных создаётся)

#### 1.3 Создание таблиц
1. В левом меню выберите **"SQL Editor"** (значок </> )
2. Нажмите **"+ New query"**
3. Скопируйте и вставьте этот SQL код:

```sql
-- Таблица игроков
CREATE TABLE players (
    telegram_id BIGINT PRIMARY KEY,
    telegram_username TEXT,
    telegram_first_name TEXT,
    valorant_nick TEXT NOT NULL,
    rank TEXT NOT NULL,
    roles TEXT[] NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица ежедневных статусов
CREATE TABLE daily_status (
    telegram_id BIGINT,
    date DATE,
    is_playing BOOLEAN DEFAULT FALSE,
    updated_at TIMESTAMP DEFAULT NOW(),
    PRIMARY KEY (telegram_id, date),
    FOREIGN KEY (telegram_id) REFERENCES players(telegram_id) ON DELETE CASCADE
);

-- Индексы для быстрого поиска
CREATE INDEX idx_daily_status_date ON daily_status(date);
CREATE INDEX idx_daily_status_playing ON daily_status(date, is_playing);
```

4. Нажмите **"Run"** (или `Ctrl+Enter`)
5. Должно появиться: ✅ `Success. No rows returned`

#### 1.4 Получение ключей доступа
1. Перейдите в **"Project Settings"** (значок ⚙️ внизу слева)
2. Выберите **"API"**
3. Скопируйте и СОХРАНИТЕ:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public key** (длинный ключ начинающийся с `eyJ...`)

📝 **ВАЖНО:** Сохраните эти данные, они понадобятся дальше!

---

### ШАГ 2: Создание Telegram бота (5 минут)

1. Откройте **Telegram**
2. Найдите **@BotFather**
3. Отправьте команду: `/newbot`
4. Введите имя бота: `VALORANT Team Finder`
5. Введите username: `valorant_team_finder_YOUR_NAME_bot` (замените YOUR_NAME на что-то уникальное)
6. **СКОПИРУЙТЕ И СОХРАНИТЕ ТОКЕН!** (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
7. Также сохраните username бота (например: `valorant_team_finder_john_bot`)

---

### ШАГ 3: Загрузка кода на GitHub (10 минут)

#### 3.1 Создание репозитория
1. Откройте https://github.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните:
   - **Repository name**: `valorant-team-finder`
   - **Description**: `VALORANT Team coordination app`
   - Выберите **Public**
5. Нажмите **"Create repository"**

#### 3.2 Загрузка файлов

**Способ 1: Через веб-интерфейс (проще)**

1. На странице вашего репозитория нажмите **"uploading an existing file"**
2. Перетащите ВСЕ файлы из папки `valorant-cloud/`:
   - Папку `bot/` с файлами
   - Папку `api/` с файлами
   - Папку `public/` с файлами
   - Файл `vercel.json`
3. Добавьте описание коммита: `Initial commit`
4. Нажмите **"Commit changes"**

**Способ 2: Через Git (если знаете как)**

```bash
git clone https://github.com/ваш-username/valorant-team-finder
cd valorant-team-finder
# Скопируйте все файлы из valorant-cloud/
git add .
git commit -m "Initial commit"
git push origin main
```

---

### ШАГ 4: Развёртывание веб-приложения на Vercel (5 минут)

#### 4.1 Подключение к Vercel
1. Откройте https://vercel.com
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"**
4. Авторизуйтесь через GitHub

#### 4.2 Импорт проекта
1. На главной странице Vercel нажмите **"Add New..."** → **"Project"**
2. Найдите ваш репозиторий `valorant-team-finder`
3. Нажмите **"Import"**

#### 4.3 Настройка переменных окружения
1. Разверните секцию **"Environment Variables"**
2. Добавьте две переменные:

**Первая переменная:**
- **Name**: `SUPABASE_URL`
- **Value**: ваш Project URL из Supabase (например: `https://xxxxx.supabase.co`)

**Вторая переменная:**
- **Name**: `SUPABASE_KEY`
- **Value**: ваш anon public key из Supabase

3. Нажмите **"Deploy"**
4. Подождите 2-3 минуты

#### 4.4 Получение URL приложения
После успешного деплоя:
1. Vercel покажет вам URL типа: `https://valorant-team-finder.vercel.app`
2. **СОХРАНИТЕ ЭТОТ URL!**
3. Откройте его в браузере - должна открыться ваша веб-страница

---

### ШАГ 5: Обновление ссылки на бота в веб-приложении (3 минуты)

1. Вернитесь в GitHub к вашему репозиторию
2. Откройте файл `public/index.html`
3. Нажмите иконку **карандаша** (Edit this file)
4. Найдите строку (около строки 435):
   ```html
   <a href="https://t.me/YOUR_BOT_USERNAME" class="telegram-link" target="_blank" id="botLink">
   ```
5. Замените `YOUR_BOT_USERNAME` на username вашего бота:
   ```html
   <a href="https://t.me/valorant_team_finder_john_bot" class="telegram-link" target="_blank" id="botLink">
   ```
6. Нажмите **"Commit changes"**
7. Vercel автоматически обновит приложение за 1-2 минуты

---

### ШАГ 6: Развёртывание бота на Railway (10 минут)

#### 6.1 Регистрация в Railway
1. Откройте https://railway.app
2. Нажмите **"Login"**
3. Выберите **"Login with GitHub"**
4. Авторизуйтесь

#### 6.2 Создание проекта
1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `valorant-team-finder`
4. Railway начнёт создание проекта

#### 6.3 Настройка бота
1. После создания проекта, в Railway:
2. Нажмите на ваш проект
3. Перейдите в **"Settings"**
4. В разделе **"Build"**:
   - **Root Directory**: `bot`
   - **Start Command**: `python main.py`

#### 6.4 Добавление переменных окружения
1. Перейдите во вкладку **"Variables"**
2. Нажмите **"New Variable"**
3. Добавьте три переменные:

**Переменная 1:**
- Key: `BOT_TOKEN`
- Value: ваш токен от BotFather

**Переменная 2:**
- Key: `SUPABASE_URL`
- Value: ваш Project URL из Supabase

**Переменная 3:**
- Key: `SUPABASE_KEY`
- Value: ваш anon public key из Supabase

4. Railway автоматически перезапустит бота

#### 6.5 Проверка работы бота
1. Во вкладке **"Deployments"** проверьте статус
2. Должно быть: ✅ `SUCCESS`
3. Откройте **"View Logs"** - должно быть написано: `Бот запущен!`

---

### ШАГ 7: ТЕСТИРОВАНИЕ! 🎉

#### 7.1 Тест бота
1. Откройте Telegram
2. Найдите вашего бота (по username)
3. Отправьте команду: `/start`
4. Бот должен ответить и попросить ввести ник

#### 7.2 Заполнение профиля
1. Введите свой ник в VALORANT
2. Выберите ранг
3. Выберите роли
4. Нажмите "Готово"

#### 7.3 Отметиться на сегодня
1. В меню бота выберите кнопку или дождитесь автоматического вопроса
2. Нажмите "✅ Да, буду играть!"

#### 7.4 Проверка веб-приложения
1. Откройте ваш URL на Vercel: `https://valorant-team-finder.vercel.app`
2. Нажмите "🔄 Обновить"
3. Вы должны увидеть свой профиль в списке играющих!

**🎉 ЕСЛИ ВСЁ РАБОТАЕТ - ПОЗДРАВЛЯЮ! СИСТЕМА ЗАПУЩЕНА!**

---

## 🔧 Дополнительные настройки

### Изменение времени уведомлений

По умолчанию бот спрашивает в 10:00 и 18:00 (по московскому времени UTC+3).

Чтобы изменить:
1. В GitHub откройте файл `bot/main.py`
2. Найдите строки (около 237-238):
   ```python
   job_queue.run_daily(send_daily_notification, time=time(10, 0, 0))
   job_queue.run_daily(send_daily_notification, time=time(18, 0, 0))
   ```
3. Измените часы. Например, для 12:00 и 20:00:
   ```python
   job_queue.run_daily(send_daily_notification, time=time(12, 0, 0))
   job_queue.run_daily(send_daily_notification, time=time(20, 0, 0))
   ```
4. Сохраните (Commit changes)
5. Railway автоматически перезапустит бота

---

## 📊 Мониторинг

### Проверка работы бота (Railway)
1. Откройте https://railway.app
2. Выберите ваш проект
3. Перейдите в **"Deployments"**
4. Нажмите **"View Logs"** - здесь все логи бота

### Проверка работы веб-приложения (Vercel)
1. Откройте https://vercel.com
2. Выберите ваш проект
3. Вкладка **"Deployments"** - статус деплоев
4. Вкладка **"Logs"** - логи запросов

### Просмотр данных в Supabase
1. Откройте https://supabase.com
2. Выберите ваш проект
3. **"Table Editor"** → `players` - все зарегистрированные игроки
4. **"Table Editor"** → `daily_status` - статусы на сегодня

---

## 🎮 Как пользоваться системой

### Для игроков:

1. **Регистрация** (один раз):
   - Открыть бота в Telegram
   - Отправить `/start`
   - Заполнить анкету

2. **Каждый день**:
   - Бот сам спросит в 10:00 и 18:00: "Будешь играть?"
   - Ответить "Да" или "Нет"

3. **Просмотр списка**:
   - В боте: кнопка "Кто играет сегодня"
   - В браузере: открыть веб-приложение

### Для администратора (вас):

Система работает **полностью автоматически**! Вам ничего не нужно делать.

Но вы можете:
- Смотреть статистику в Supabase
- Смотреть логи в Railway и Vercel
- Менять время уведомлений в коде

---

## 🐛 Решение проблем

### Проблема: Бот не отвечает

**Проверка:**
1. Откройте Railway → ваш проект → Deployments
2. Статус должен быть `SUCCESS`
3. В логах должно быть: `Бот запущен!`

**Решение:**
- Проверьте переменные окружения в Railway
- Убедитесь, что `BOT_TOKEN` правильный
- Перезапустите деплой: Settings → Redeploy

### Проблема: Веб-приложение не загружает игроков

**Проверка:**
1. Откройте браузер → F12 → вкладка Console
2. Смотрите на ошибки

**Решение:**
- Проверьте переменные окружения в Vercel
- Убедитесь, что `SUPABASE_URL` и `SUPABASE_KEY` правильные
- Redeploy в Vercel

### Проблема: Игроки не сохраняются

**Проверка:**
1. Откройте Supabase → SQL Editor
2. Выполните: `SELECT * FROM players;`
3. Проверьте есть ли данные

**Решение:**
- Проверьте что таблицы созданы правильно
- Проверьте логи бота в Railway

---

## 💰 Лимиты бесплатных планов

### Supabase (Free):
- ✅ 500 MB база данных (хватит на 10,000+ игроков)
- ✅ Неограниченное API
- ✅ Навсегда бесплатно

### Vercel (Free):
- ✅ 100 GB bandwidth/месяц
- ✅ Неограниченное кол-во запросов
- ✅ Навсегда бесплатно

### Railway (Free):
- ⚠️ $5 кредитов в месяц
- ✅ Хватит на ~500 часов работы (весь месяц 24/7)
- Если кредиты закончатся: бот остановится, но данные сохранятся

**Альтернатива Railway (если кредиты закончатся):**
- Render.com (бесплатно, но спит после 15 мин неактивности)
- Fly.io (бесплатно, лимиты выше)

---

## ✅ Финальный чеклист

- [ ] Создан проект в Supabase
- [ ] Созданы таблицы в Supabase
- [ ] Получены ключи Supabase (URL и KEY)
- [ ] Создан бот в @BotFather
- [ ] Получен токен бота
- [ ] Создан репозиторий на GitHub
- [ ] Все файлы загружены на GitHub
- [ ] Проект задеплоен на Vercel
- [ ] Переменные окружения добавлены в Vercel
- [ ] Ссылка на бота обновлена в index.html
- [ ] Проект задеплоен на Railway
- [ ] Переменные окружения добавлены в Railway
- [ ] Бот работает (проверено в логах)
- [ ] Веб-приложение открывается
- [ ] Тестовая регистрация прошла успешно
- [ ] Тестовое отмечание работает
- [ ] Игрок виден в веб-приложении

**Если все галочки стоят - ПОЗДРАВЛЯЮ! 🎉**

**Ваша система работает 24/7 автоматически!**

---

## 📞 Дополнительные ресурсы

- Документация Supabase: https://supabase.com/docs
- Документация Vercel: https://vercel.com/docs
- Документация Railway: https://docs.railway.app
- Telegram Bot API: https://core.telegram.org/bots

---

## 🎯 Что дальше?

Система полностью готова! Теперь:

1. **Пригласите друзей** - дайте им ссылку на бота
2. **Делитесь ссылкой** на веб-приложение
3. **Наслаждайтесь** - всё работает автоматически!

Бот будет каждый день спрашивать всех игроков, а веб-приложение будет показывать актуальный список.

**Удачной игры в VALORANT! 🎮🔥**
