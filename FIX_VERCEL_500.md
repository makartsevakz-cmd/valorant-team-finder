# 🔧 Исправление ошибки 500 в Vercel - Пошаговая инструкция

## ✅ Переменные окружения добавлены правильно!

Я вижу что вы правильно добавили:
- `SUPABASE_URL` = `https://kpijizuasmaprqjpieyl.supabase.co`
- `SUPABASE_KEY` = есть (скрыт)

Теперь нужно **обновить код API** на GitHub и сделать redeploy.

---

## 📝 Что нужно сделать (5 минут):

### Шаг 1: Обновите файлы на GitHub

Нужно заменить три файла в вашем репозитории:

#### 1.1 Замените `vercel.json`

Откройте в GitHub: `vercel.json`

Замените содержимое на:
```json
{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
```

#### 1.2 Замените `api/index.py`

Этот файл полностью переписан под Vercel.

Скачайте обновлённый файл из архива `valorant-cloud-project.tar.gz` или скопируйте новый код.

#### 1.3 Переместите `index.html`

- Скопируйте `public/index.html` в корень проекта (просто `index.html`)
- Или переименуйте папку `public` → удалите её, а `index.html` оставьте в корне

**Итоговая структура:**
```
valorant-team-finder/
├── api/
│   ├── index.py          ← ОБНОВИТЬ!
│   └── requirements.txt
├── bot/
│   ├── main.py
│   ├── database.py
│   └── requirements.txt
├── index.html            ← В КОРНЕ!
├── vercel.json           ← ОБНОВИТЬ!
└── другие файлы...
```

### Шаг 2: Дождитесь автодеплоя в Vercel

После коммита на GitHub:
1. Vercel автоматически начнёт новый деплой (1-2 минуты)
2. Следите за статусом в Vercel → Deployments

### Шаг 3: Проверьте API

Откройте в браузере:
```
https://valorant-team-finder-1.vercel.app/api/health
```

**Должно показать:**
```json
{
  "success": true,
  "status": "healthy",
  "timestamp": "2026-01-26...",
  "supabase_connected": true,
  "supabase_url": "https://kpijizuasmaprqjpieyl.supabase.co"
}
```

Если `"supabase_connected": true` - отлично! ✅

### Шаг 4: Проверьте веб-приложение

Откройте главную страницу и нажмите "🔄 Обновить"

Данные должны загрузиться!

---

## 🎯 Быстрый способ (рекомендуется):

### Вариант A: Загрузить весь архив заново

1. Скачайте обновлённый `valorant-cloud-project.tar.gz`
2. Распакуйте
3. Замените все файлы в вашем GitHub репозитории
4. Commit → Push
5. Vercel автоматически задеплоит

### Вариант B: Через веб-интерфейс GitHub

1. Откройте ваш репозиторий на GitHub
2. Для каждого файла (`vercel.json`, `api/index.py`):
   - Нажмите на файл
   - Нажмите карандаш (Edit)
   - Замените содержимое
   - Commit changes
3. Vercel автоматически задеплоит

---

## 🐛 Если всё ещё ошибка 500:

### Проверьте в Vercel → Deployments → Logs:

Найдите строки с ошибками (красные).

**Частые причины:**
1. `ModuleNotFoundError: No module named 'supabase'`
   - Решение: Убедитесь что `api/requirements.txt` содержит `supabase>=2.0`

2. `KeyError: 'path'` или похожие
   - Решение: Используйте обновлённый `api/index.py` из архива

3. Ошибка подключения к Supabase
   - Решение: Проверьте что SUPABASE_KEY правильный (должен начинаться с `eyJ`)

---

## ✅ Чеклист:

- [ ] Переменные окружения добавлены в Vercel ✅ (у вас уже есть!)
- [ ] Файл `vercel.json` обновлён
- [ ] Файл `api/index.py` обновлён  
- [ ] Файл `index.html` в корне проекта
- [ ] Commit на GitHub сделан
- [ ] Vercel автоматически задеплоил
- [ ] `/api/health` показывает success: true
- [ ] Веб-приложение показывает данные

---

## 💡 Почему нужно обновить код:

Старый код API использовал формат несовместимый с Vercel Python Runtime.

Новый код:
- ✅ Правильно обрабатывает Vercel события
- ✅ Корректно работает с Supabase
- ✅ Возвращает JSON в правильном формате
- ✅ Имеет подробные сообщения об ошибках

После обновления всё заработает! 🚀
